# Alertmanager configuration for Minder
global:
  resolve_timeout: 5m

# Route configuration
route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 12h
  receiver: 'default'

# Receivers
receivers:
  - name: 'default'
