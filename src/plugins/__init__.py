"""First-party module plugins for Minder.

Loaded by the plugin-registry disk-loader on startup. Each subpackage
(crypto, network, news, tefas, telegraf, weather) implements the module-plugin
contract defined in ``_contract.py``.
"""
