# Core Application Requirements
fastapi==0.141.1
pydantic==2.13.4
httpx==0.28.1
asyncpg==0.31.0
redis==8.1.0

# AI/ML Libraries
ollama==0.6.2
qdrant-client==1.19.0
neo4j==6.2.0

# Server
uvicorn==0.52.3

# Configuration & Settings
pydantic-settings==2.15.0
python-dotenv==1.2.3

# Utilities
python-multipart==0.0.32
# BOTH JWT libraries, matching api-gateway/requirements.txt (#797): PyJWT for
# shared.auth.jwt_middleware's HS256 session JWTs; python-jose stays because
# api-gateway's core/oidc.py relies on its OIDC at_hash claim check, which
# PyJWT has no equivalent for. This is the general dev/test requirements
# file, so it needs both to import/test every service's code.
PyJWT[crypto]==2.13.0
python-jose[cryptography]==3.5.0

# Monitoring
prometheus-client==0.26.0

# Database
psycopg2-binary==2.9.12

# File & Data Handling
aiofiles==25.1.0
aiohttp==3.14.3
pyyaml==6.0.3
pyjwt==2.13.0

# HTTP Client (test dependency)
requests==2.34.2

# System & Performance Testing
psutil==7.2.2
locust==2.46.3

# Password Hashing
passlib[bcrypt]==1.7.4
