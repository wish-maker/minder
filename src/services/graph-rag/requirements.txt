fastapi==0.141.1
uvicorn==0.52.3
pydantic==2.13.4
pydantic-settings==2.15.0
spacy==3.8.15
neo4j==6.2.0
python-multipart==0.0.32
# Auth (shared.auth.jwt_middleware -- get_current_user_or_service, added for #405).
# PyJWT, not python-jose (#797): this service only ever verifies our own
# HS256 session JWTs via jwt_middleware.py, never Authelia's OIDC RS256
# tokens (that's api-gateway-only, core/oidc.py) -- so it doesn't need
# python-jose's ecdsa/pyasn1/rsa transitive deps at all.
PyJWT[crypto]==2.13.0
prometheus-client==0.26.0
