# Plugin Registry Dependencies
# FastAPI framework
fastapi==0.141.1
uvicorn==0.52.3
pydantic==2.13.4
pydantic-settings==2.15.0

# Database
asyncpg==0.31.0

# Redis
redis==8.1.0

# Plugin system
pyyaml==6.0.3
jsonschema==4.26.0

# Plugin Dependencies
httpx==0.28.1

# Monitoring
prometheus-client==0.26.0

# Utilities
# PyJWT, not python-jose (#797): this service only ever verifies our own
# HS256 session JWTs via shared.auth.jwt_middleware, never Authelia's OIDC
# RS256 tokens (api-gateway-only) -- doesn't need python-jose's
# ecdsa/pyasn1/rsa transitive deps.
PyJWT[crypto]==2.13.0

# Plugin-specific dependencies (TEFAS, time-series)
tefas-crawler==0.6.0
# Not imported by any current plugin, but deliberately pre-installed for a
# planned future BIST plugin (see src/plugins/tefas/__init__.py's own
# docstring) -- kept, unlike this issue's other genuinely-dead pins (#341).
borsapy==0.11.0
